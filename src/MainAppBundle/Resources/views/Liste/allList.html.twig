{% extends "@MainApp/base.html.twig" %}

{% block body %}
    <table class="table table-hover table-dark">
        <thead class="">
            <tr>
                <th scope="col">
                    Nom
                </th>
                <th scope="col">
                    Points
                </th>
            </tr>
        </thead>
        <tbody>
            {% for liste in listArray  %}
                    <tr class="choiceList" data-listid="{{ liste.id }}">
                        <td>
                            {{ liste.name }}
                        </td>
                        <td>
                            {{ liste.pointsLimit }} pts
                        </td>
                    </tr>
                </a>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
